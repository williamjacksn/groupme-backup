[project]
name = "groupme-backup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fort>=2025.0",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.1",
]
